apiVersion: v2
name: grafana-dashboard-cloner
description: Tool that will clone dashboards (based on tags) from one Grafana instance to another

type: application

version: 0.1.2

appVersion: "1.0.1"
